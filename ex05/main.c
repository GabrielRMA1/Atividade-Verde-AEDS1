#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int sfinal,termo,posicao,razao;
    scanf("%i",&razao);
    scanf("%i",&termo);
    sfinal=termo*pow(razao,);
    printf("%i",sfinal);
    return 0;
}
