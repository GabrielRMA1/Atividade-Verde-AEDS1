#include <stdio.h>
#include <stdlib.h>

int main()
{
    int sfinal,termo,posicao,razao;
    scanf("%i",&razao);
    scanf("%i",&termo);
    sfinal=termo+(10-1)*razao;
    printf("%i",sfinal);
    return 0;
}